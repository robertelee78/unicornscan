# Core scanner binaries and libraries (installed by make install)
# These are handled by dh_auto_install

# Alicorn Web UI components are installed by override_dh_auto_install in debian/rules
# to FHS-compliant paths:
#   Static files:  /usr/share/unicornscan/alicorn
#   Runtime data:  /var/lib/unicornscan/alicorn
