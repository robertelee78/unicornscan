# Core scanner binaries and libraries (installed by make install)
# These are handled by dh_auto_install

# Alicorn Web UI - Docker Compose and config (handled by rules override)
# Note: docker-compose.yml, Dockerfile, nginx.conf, package.json, etc.
# are installed by the override_dh_auto_install target in debian/rules

# PostgREST configuration
alicorn/postgrest                      opt/unicornscan-web/
